interface:
  display_name: "Init Context"
  short_description: "Scaffold or refresh a repo's AGENTS.md"
  default_prompt: "Use $init-context to create or update AGENTS.md with project-specific rules, concrete verification commands, documentation norms, and key architectural decisions."
