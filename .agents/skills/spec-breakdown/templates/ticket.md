# {{TITLE}}

<One-paragraph summary: what exists after this ticket is done and why it
matters. This whole file below the H1 becomes the issue body when published,
so write it for someone (or some agent) picking up only this ticket.>

- **Spec**: `docs/specs/{{SPEC_SLUG}}.md` <— name the sections that govern
  this ticket, e.g. "Design › Control flow: token refresh", rather than
  restating them>
- **Implements**: <FR-n / NFR-n from the spec>
- **Depends on**: none <or T-n with one clause on what it needs from it>

## Context

<The minimum a fresh implementor needs that the spec sections above don't
say: current behavior being changed, constraints already decided, gotchas.>

## Work

<The shape of the change in a few lines — components touched, new
interfaces, data changes. Direction, not file-level edit instructions.>

## Acceptance

- [ ] <Observable check inherited/refined from the spec's acceptance
  criteria — verifiable by someone who didn't build it.>
