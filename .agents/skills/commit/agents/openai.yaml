interface:
  display_name: "Commit"
  short_description: "Stage related changes and create clean conventional commits"
  default_prompt: "Use $commit to stage related changes and create a clean commit message."
